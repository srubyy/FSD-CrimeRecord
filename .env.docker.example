# Environment Variables Template for Docker Compose Setup
# Copy this file to .env.docker before running docker-compose up --build

JWT_SECRET=crimenet_docker_jwt_super_secret_key_2026
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173
PORT=5001
