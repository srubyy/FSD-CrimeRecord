# Server Configuration
PORT=5000

# MongoDB Connection (MANDATORY - No hardcoded fallback credentials exist in codebase)
MONGO_URI=mongodb://127.0.0.1:27017/crimenet

# JWT Secret Key for Authentication Tokens (MANDATORY - No hardcoded fallback exists)
JWT_SECRET=crimenet_super_secret_jwt_key_2026

# CORS Allowed Origins (Comma-separated list)
ALLOWED_ORIGINS=http://localhost:5173,https://fsd-crime-record.vercel.app
